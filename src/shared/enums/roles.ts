export enum UserRole {
  ADMIN = 'admin',
  AGENT = 'agent',
}

export enum InterventionType {
  MEDICAL = 'medical',
  FIRE = 'fire',
  TRAFFIC = 'traffic',
  STRUCTURAL = 'structural',
  OTHER = 'other',
}

export enum ReportStatus {
  STEP_1 = 'step_1',
  COMPLETED = 'completed',
}
